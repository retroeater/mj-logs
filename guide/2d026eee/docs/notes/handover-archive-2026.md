# handover.md / CLAUDE.md から移した履歴（2026年）

`docs/handover.md` と `CLAUDE.md` から、完了済みで今後の作業の前提にならない経緯を移したもの。
判断基準は「その記述を読まなくても次のセッションが作業を始められるか」。
規則そのものは移していない（規則は `CLAUDE.md` と `docs/handover.md` が正）。

2026-09-14 に移動（Chat-Ref: CHAT-0914-RV-07）。本文は移動元の記述をそのまま残している。

---

## docs/handover.md から

### 指示文中のラベル指定が前提と食い違った例（CHAT-0913-AR-01）

（移動元: handover.md「指示文中のラベル指定が前提と食い違った例（CHAT-0913-AR-01）」、移動時点の 265〜272 行目）

**指示文中のラベル指定は、前提と食い違うことがある。**
2026-09-12 のレビュー起票（CHAT-0913-AR-01）で、ある issue の指示文が
「対象: 全ページ」を指定しながら、同じ指示文の前提に「『対象:』に該当する
ラベルがないページは対象ラベルを付けない」と書かれており矛盾していた。
Claude Code 側は前提を優先し、分野ラベルのみで起票した（#180。対象は
houou_leagues など5ページで、いずれも対応する「対象:」ラベルが存在しない）。
**この判断でよい。** 指示文の記述同士が食い違ったときは、個別の指定より
前提・ルールの側を優先し、その旨を報告すること。

### 複数セッションの並行作業とブランチ運用が決まるまでの経緯（#198/#205/#207/#209）

（移動元: handover.md「複数セッションの並行作業とブランチ運用が決まるまでの経緯（#198/#205/#207/#209）」、移動時点の 274〜327 行目）

### 複数セッションが共有するのはissueだけでなくワーキングツリーも（2026-09-12）

Codespaceを共有したまま複数のClaude Codeセッションを動かすと、片方の
作業中ファイルが、もう片方の `git status` に「出所不明の未コミット変更」
として現れる。実例: あるセッションが `docs/handover.md` の未コミット差分を
検出したが、これは別セッションが#26/#108の記述修正を作業中だったもので、
その後 `c4083c4` として正しくコミットされた。

このとき危ないのは次の2つ。

- 出所不明の差分を「不要」と判断して `git checkout` で捨てる
  → 他セッションの作業が消える
- `git add -A` でコミットする → 他セッションの作業中ファイルを巻き込む

**身に覚えのない未コミット変更を見つけたら、捨てる前に必ず `git diff` で
中身を確認し、他セッションの作業でないかを疑うこと。**
#157の着手宣言（issueコメント）はissue単位の二重着手を防ぐ仕組みで、
別々のissueを扱っていてもファイルは1つのツリーを共有するため、この
問題は防げない。

**恒久対策として、セッションごとに作業ブランチを分ける運用（A案）を
2026-09-13に決定した（#198）。** `cloudflare`は統合・デプロイ専用とし、
セッションは`work/<セッション識別子>`で作業してそこへpushする。
`cloudflare`へのマージは平野さんが判断する（マージ＝本番反映）。
`git stash`は共有パス上で他セッションの未コミット編集を無言で消しうる
ため使用しない（`git add -p`または`git apply --cached`で部分ステージ
する）。

**ただしA案には欠陥があった。** 全セッションが`/workspaces/mj`という
単一ディレクトリを共有しているため、**ブランチを分けても作業ツリーは
分かれない。** あるセッションが`git checkout`すると、他セッションの
足元のブランチも同時に切り替わる。実際にWH-22のマージ作業中、
チェックアウト先が別セッションの作業ブランチへ無断で切り替わる事象が
起きた。対策として、作業は`git worktree add`で作った専用ディレクトリで
行い、`/workspaces/mj`ではブランチ切り替えを行わないことにした
（2026-09-13、#198）。ルールの詳細はCLAUDE.mdの「ブランチ運用」に記載
（二重管理を避けるためここには複製しない）。

**チャット側の指示文が古い前提を含んでいたことが原因で、`cloudflare`への
直接pushが実際に発生した（`1b8eec6`、2026-09-13）。** チャット側の指示文が
ブランチ運用ルール制定前の書き方のままだったことが一因。CLAUDE.mdのルールを
指示文より優先する旨を明記して対応した（#205）。経緯・対応の詳細は#205参照。

**ブランチ削除は削除直前のSHAを記録しないと後から検証できない。**
#207（`claude/*`3本の削除）はSHAを記録せず、#206は指示文作成時点の
SHA（`953c19e`）が実行時には`c54277c`まで進んでいたことが後から判明した
（実行時に完全な履歴で再確認したため実害は無かった）。削除直前のSHAを
必ず記録する運用に改めた（#209）。詳細は#209参照。

**祖先関係だけでは「積み直してマージ済み」を検出できない。**
`work/0913-hv`（元`feae0e3`、#209直後にリモートから削除確認）は、
`cloudflare`の祖先ではなかったが、積み直し後`2deb7d4`として同一内容が
既に入っていた。祖先関係で未マージと即断せず、件名・差分を突き合わせて
から判定する運用に改めた（#209）。

### #169で何を誤ったか

（移動元: handover.md「#169で何を誤ったか」、移動時点の 459〜476 行目）

### #169で何を誤ったか

#169では「セッション環境から `api.cloudflare.com` に到達できず、Cloudflare
の設定を確認できない」ことを「本番反映の経路が存在しない」と誤って結論づけ、
`.github/workflows/deploy.yml` を追加して`npx wrangler deploy`を直接叩く
ようにしていた。実際にはWorkers Buildsが既に稼働しており、この追加は
不要かつ有害（二重デプロイになりうる）だった。

- 到達できないことと存在しないことは別。確認できない領域については
  「仕組みが存在しない」という結論を出すべきではなかった
- `gh api repos/retroeater/mj/commits/<sha>/check-runs` でGitHub上の
  check-runsを見れば、Cloudflareダッシュボードに入らずに「Workers Builds: mj」
  というアプリのcheck-runが記録されていることが確認できた。実際、#153の
  検証（2026-09-11）ではこの方法でWorkers Buildsの稼働を確認済みだったが、
  #169（2026-09-12）はこれを確認せずに着手した
- 対応: `deploy.yml`は削除し、デプロイ要素を除いた検査専用の
  `.github/workflows/assets-check.yml`として残した（内容は後述）。
  `CLOUDFLARE_API_TOKEN`のSecretは登録していない（二重デプロイになるため）

### APIトークン整理後の初回ビルドとリネーム表示（2026-09-12）

（移動元: handover.md「APIトークン整理後の初回ビルドとリネーム表示（2026-09-12）」、移動時点の 523〜532 行目）

- 2026-09-12、4本→1本へ整理した後の初回ビルド（Build `#5c2974bf`、対象
  コミット`1f7f76d`、ブランチ`cloudflare`）が成功（所要40秒）し、残した
  1本で本番反映が通ることを確認済み（GitHubのcheck-runsで確認。上記
  「セッション環境からは Cloudflare に到達できない」節の区別のとおり、
  確認できたのは「ビルド成功」であって本番の見え方ではない）
- 整理後、残したトークンは `mj build token (Workers Builds)` にリネーム
  済み。**ただしDeploymentsのビルド詳細（Build settings）に表示される
  Build token名は`mj build token`のままだった。** リネームの反映に時間差が
  あるのか、ビルド実行時点の名前を保持しているのかは不明。別のトークンに
  差し替わったわけではなく、同じトークンの表示上のラグと見られる（事実として記録）

### docs のみの push で「連携が切れた」と誤判断した例（#211）

（移動元: handover.md「docs のみの push で「連携が切れた」と誤判断した例（#211）」、移動時点の 598〜599 行目）

2026-09-13、private 化（#211）直後に docs のみの `a986f46` で check-run が
出ないのを「連携が切れた」と誤判断した（#211 の補足コメント）。

### 「check-run が queued のまま」の誤報（AR-18）

（移動元: handover.md「「check-run が queued のまま」の誤報（AR-18）」、移動時点の 613〜618 行目）

**「check-run が queued のまま」を「デプロイが詰まっている」と報告しない
こと。** 2026-09-13 に AR-18（`docs/notes/a11y-manual-check.md` 追加の
コミット `effe638`）でこの誤報が発生し、ダッシュボードを確認したところ
実際には直近11件すべて成功しており滞留はなかった。`570928b`（AR-17）と
`effe638`（AR-18）は Build history に個別の行を持たず、後続コミットの
ビルドに内容ごと取り込まれていた（AR-11・AR-14 でも同じ現象を観測済み）。

### 並行セッションとの関係を会話の記憶から断定した例（CHAT-0914-YT-08）

（移動元なし。2026-09-14 に直接記録。Chat-Ref: CHAT-0914-YT-08）

2026-09-14、YT-01〜07（#3 / #326）の振り返りで、チャット側が
「この間、他セッションと衝突していない」と会話の記憶から断定したが、
Claude Code の報告と `origin/cloudflare` の履歴（first-parent）とは食い違っていた。

- YT-01 の着手時点で、`cloudflare` は会話開始時の `a1fbffe` より先行しており
  （#312 のマージ等）、worktree は `b7656d6` から切った
- YT-02 の着手前にも `c8b71dd`〜`b28a37a` が先行しており、rebase してから作業した
  （YT-03・YT-04 でも先行分を rebase）
- YT-05 のマージ `de087bb` から YT-06 の土台 `8b5b136` までの間に、別セッション（W2）の
  `15ab188` / `378849e` と全ページ再生成の `8b5b136` が入っていた
- YT-06 の報告は、別チャットの指示（CHAT-0914-W2-04）と同時に答えたもの

コンフリクトは起きていないが、「並行する変更が無かった」わけではなく、
ブランチの進み方は YT のコミットだけでは説明がつかない。AR-18 の誤報と同じ型。
**コミットの有無・SHA の前後関係は会話の記憶から書かず、Claude Code に確認させるか
実物（`git log`）を見ること。**

### アクセシビリティの静的レビュー（2026-09-12、#178〜#185）の指摘一覧

（移動元: handover.md「アクセシビリティの静的レビュー（2026-09-12、#178〜#185）の指摘一覧」、移動時点の 783〜820 行目）

### アクセシビリティの静的レビュー（2026-09-12、#178〜#185）

※ #178〜#186 が全件クローズしたら、この節は docs/notes/site-findings.md
　 へ移し、ここには結論1行と参照だけを残す

チャット側で全ページのアクセシビリティ静的レビューを行い、8件を起票した。
**実機のスクリーンリーダー・axe-core・Lighthouse による検証はしていない。**
コードの読解のみで判定しているため、実装前に各 issue で実機確認すること
（通し確認そのものは #186 として起票済み）。

| # | 内容 | 再生成 |
|---|---|---|
| #178 | navbar.js の `id="navbarDropdown"` 7重複・英語 aria-label | 不要 |
| #179 | 固定ナビ下にフォーカスが隠れる（`scroll-padding-top` 1行） | 不要 |
| #180 | `select#selectbox` のラベル欠落・選択即遷移（5ページ） | leagues 2ページのみ要 |
| #181 | jpml_pros の名前セルを `th scope="row"` | 要 |
| #182 | `<main>` とスキップリンクをテンプレートに追加（25ページ） | 要（全ページ） |
| #183 | `target="_blank"` 16,699件に別タブの予告がない | 要（全ページ） |
| #184 | ページ送りが disabled になるとフォーカスが消える | 不要 |
| #185 | index.html のモバイルナビ開閉が `<i>` でキーボード操作不可 | 不要 |

**まとめ方の目安**

- #178 / #179 / #184 は単一ファイルの修正で再生成不要。1セッションで片付く
- #181 / #182 / #183 は `lib/page.py`・`generate_jpml_pros.py` を触るため
  全ページ再生成を伴う
- #180 のランキング3ページ分は #141 の移行要件に含めた（#111 にも同じ
  要件をコメント済み）。leagues 2ページだけ先に対応できる

**Lighthouse で拾えていない指摘がある。** #163 の対応後、生成済みページの
accessibility は 0.98〜1.00 で残指摘は landmark-one-main のみという状態
だったが、#178 はその状態でも存在していた。**スコアを到達点として
扱わないこと。**

**#183 は実装前に方針判断が要る。** (a) visually-hidden の予告テキストを
足す / (b) そもそも別タブをやめる、のどちらか。16,699件すべてに影響する。

実機での通し確認（#186）用のチェックリストは `docs/notes/a11y-manual-check.md` にある。

### 2026-09-14 追加移動（Chat-Ref: CHAT-0914-WH-07）

handover.md 単独が警告域（51,200バイト）の手前だったため、第2便・第3便の追記余地を作る目的で次の4節を移した。本文は移動元の記述をそのまま残している。

### APIトークンの棚卸し（2026-09-12）

（移動元: handover.md「APIトークンの棚卸し（2026-09-12）」、移動時点の 476〜492 行目）

### APIトークンの棚卸し（2026-09-12）

**以下もセッションからは検証できない。平野さんがダッシュボードで確認・
操作した結果をそのまま記録する。**

- Workers Builds はリポジトリを接続するたびに User API Token を自動発行する。
  同名（`mj build token`）で増えるため、接続をやり直したら古いものを削除すること。
  2026-09-12 に4本→1本へ整理した
- 現役のトークンは平野さんのアカウント（`<平野さんのメールアドレス>`）に紐づく **User API Token**。
  Cloudflare は Account API Token を推奨しているが、Workers Builds が
  自動発行するのは User Token なので選べない。
  **このユーザーのアカウントが使えなくなると本番反映が止まる**、という依存がある
- 権限は Cloudflare が決めた範囲（25権限・All zones・無期限）で、
  `wrangler deploy` に必要な範囲を大きく超えている。
  **手で絞ると次のビルドが壊れる可能性があるため触らないこと**
- 整理後の初回ビルド成功と、リネームの表示ラグの記録は `docs/notes/handover-archive-2026.md`

### check-run が queued のまま・見当たらない場合（2026-09-13）

（移動元: handover.md「check-run が queued のまま・見当たらない場合（2026-09-13）」、移動時点の 545〜572 行目）

### check-run が queued のまま・見当たらない場合（2026-09-13）

短時間に連続して push すると、Cloudflare Workers Builds は複数コミットを
1回のビルドにまとめる。**まとめられた側のコミットには check-run が
付かないため、`gh api repos/retroeater/mj/commits/<sha>/check-runs` では
`queued` のまま、または結果が無いように見える。** これはビルドの失敗でも
遅延でもない。

**`docs/**` のみの変更では Workers Builds はそもそも走らない。** Build watch
paths の Exclude paths に `docs/**` が入っているため（上記「セッション環境からは
Cloudflare に到達できない」節）、check-run が出なくても異常ではない。
デプロイの成否を確認したいときは、`docs/` 以外を含む push で確認すること。

判定の手順:

0. 変更が `docs/**` のみなら、check-run が無いのが正常。以下は不要
1. 自分のコミットに check-run が無い／queued のままでも、**その後に
   push された後続コミットの check-run を見る。** success なら自分の
   変更もそのビルドに含まれてデプロイ済み
2. それでも不明なら、Cloudflare ダッシュボードの Build history を見る
   （平野さんの作業。セッションからは `api.cloudflare.com` も `ryoei.pro` も
   遮断されている）
3. サイトのファイルを変更した場合は、本番の該当ページで反映を直接確認する
   のが最も確実

**「check-run が queued のまま」を「デプロイが詰まっている」と報告しない
こと。** 2026-09-13 の AR-18 での誤報の記録は `docs/notes/handover-archive-2026.md`

### #7の期待値の修正（2026-09-11、Lighthouse実測を受けて）

（移動元: handover.md「#7の期待値の修正（2026-09-11、Lighthouse実測を受けて）」、移動時点の 635〜668 行目）

**#7の期待値の修正（2026-09-11、Lighthouse実測を受けて）**

これまで「Charts依存の解消＝パフォーマンス改善」として進めてきたが、
実測で前提が変わった。

- **「移行済みだから速い」は成り立たない。データ件数に依存する。**
  Lighthouse実測（`docs/lighthouse-baseline.md`、2026-09-11）で判明
- 件数が少ないページは移行でTBT・LCPともに改善する
  （`jpml_test`: mobile perf 92 / TBT 0ms）
- 件数が多いページは移行しても解決しない。むしろ悪化する
  （`jpml_pros`: mobile perf 37 / TBT 1,902ms / LCP 5,642ms /
  DOM 26,886要素 / メインスレッド専有10.4秒）
- 原因はDOM要素数。現行のページ送り（`.mj-pager`）は非表示行を
  `row.hidden = true` で隠すだけでDOMからは削除していないため、
  全行のコストが常にかかる。**Google Chartsの Table chart は
  `page: 'enable'` で本当にページ単位のDOM描画に留めており、
  この点で現行の自前実装より優れている**（#7残りページの行数調査、
  `docs/lighthouse-baseline.md`で確認）
- **ただし #7 の目的はパフォーマンスだけではない。** 外部ドメイン依存の
  解消（#9 の前提）とインラインハンドラの排除は、件数に関わらず達成される。
  #7 は続ける
- 件数の多いページの根本解決は表示件数を絞ること（#24の五十音タブ、
  新サイトで対応）。既存のINP 458msの記録と同根の問題
- **DOM規模の最大の懸念ページは`houou_results`（15,416行）。** `?name`は
  任意で、未指定時は表(`myTable`)が無条件で描画されるが、現在は
  Google Chartsの`page:'enable'`+`pageSize:500`がDOMをページ単位で
  分割しているため500行に収まっている。自前の`row.hidden`方式に置き換えると
  この行数がそのままDOMに乗るため、**#7最大のDOM規模ページになる見込み**。
  `ouka_results`/`wrc_results`も同様に`?name`任意だが行数は少ない
  （1,500〜1,600台）で実害は小さい。`saikyo_results`は2026-09-11に
  移行済みで、`page_size=100`の`.mj-pager`を使うため「2,560行を無条件で
  全件描画」の懸念は解消済み。詳細は`docs/lighthouse-baseline.md`の
  行数調査表を参照

### #7: 列ヘッダのソート方針と `.mj-table-2col` / `.mj-table-3col` の適用

（移動元: handover.md「#7: 列ヘッダのソート方針と `.mj-table-2col` / `.mj-table-3col` の適用」、移動時点の 674〜693 行目）

**列ヘッダによるソートは `jpml_pros.html` 専用の機能とする。**
型Aの他ページには既定で載せず、必要と判断したページにだけ個別に
追加する方針にした（基本なし、明示的に指定があったときだけ追加）。
Google Charts版のTable chartは既定でソート可能だったため、これは
意図的な機能削減にあたる。既定の並びがシート順（日付の新しい順）で、
絞り込みと `?name=`（またはページ内の絞り込み欄）で目的の行に到達できる
ため、影響は小さいと判断した。`jpml_titles.html` はこの方針の最初の
適用例で、`jpml_test.html` / `resource_logs.html` / `video_live.html` も
同様にソート機能を持たない。

**型Aの2列ページ（画像 + 概要）には `.mj-table-2col` を付ける。**
画像列を168px固定、概要列を残り幅に伸縮させ、概要は折り返す
（style.cssの`.mj-table-2col`）。`.mj-table`本体は変えず修飾クラスとして
追加したため、15列の`jpml_pros`（`table-layout: fixed` / `width: 934px`
のまま）には影響しない。
移行済みの8ページ（`jpml_titles` / `jpml_test` / `video_live` /
`resource_logs` / `video_en` / `rh_paifu` /
`saikyo_mens` / `saikyo_results`）に適用済み。`video_mtsuku`のみ3列のため、
新設した`.mj-table-3col`（画像列168px固定＋残り2列を折り返し）を使う。
`video_wayhome`は#102第2段で表自体を廃止したため対象外。

### 2026-09-14 追加移動（Chat-Ref: CHAT-0914-W2-06）

handover.md と CLAUDE.md の合計が警告域（87,040バイト）の手前だったため、第3便の前に余裕を作る目的で次の3か所を移した。本文は移動元の記述をそのまま残している。

### #7 の型別の進捗表

（移動元: handover.md「5. 次にやること」→「#7 の進め方（検討済み）」の型別の表と注記）

対象の21ページ(15ページ完了・残6)は5つの型に分かれる。

| 型 | ページ数 | 内容 | 該当ページ |
|---|---|---|---|
| A. 表とフィルターのみ | 12(**完了9・残3**) | `jpml_pros` と同じ構造。移行しやすい | `jpml_titles`(完了)、`jpml_test`(完了)、`resource_logs`(完了)、`video_live`(完了)、`video_en`(完了)、`rh_paifu`(完了)、`saikyo_mens`(完了)、`video_mtsuku`(完了)、`saikyo_results`(完了)、ランキング3(残り) |
| A'. 多列テーブル（表のみ） | 2(**完了2・残0**) | `jpml_pros`と同じ表構成だが6〜8列あり、`.mj-table-2col`/`.mj-table-3col`がそのままでは使えない（#109）。**完了** | `rh_results`(完了、12行・6列) / `rh_results_detail`(完了、321行・8列) |
| B. 表＋ローソク足 | 3 | `Dashboard`(名前/期/リーグの`ControlWrapper`。ページごとに構成が違う) + `Table`(`page:'enable'`) + `?name`時のみ`CandlestickChart`。型A/A'と同じ手順では表を静的化できない（#111） | `houou_results` / `ouka_results` / `wrc_results` |
| C. 縦棒グラフ | 2(**完了2・残0**) | `ColumnChart`(積み上げ棒は全員共通、`?name`時に選手の折れ線1本を追加。静的SVG+折れ線だけクライアント描画のハイブリッドで移行（#127、**完了**）) | `houou_leagues`(完了) / `ouka_leagues`(完了) |
| D. 横棒グラフ | 1(**完了1・残0**) | `BarChart`。URLパラメータに依存せずデータも34行で固定。グラフ系で唯一、静的SVG化が成立する（#128、**完了**） | `resource_efficiency`(完了) |

※ ランキング3ページは `league_ranking.js`（772行）を共用している。
　 レーダーチャートの指標もこのファイルの集計ロジックを使う（新サイト）

### 外部ドメイン依存の詳細（2026-09-11〜12時点）

（移動元: handover.md「4. 押さえておくべき方針」→「外部ドメインへの依存を増やさない」の表より後ろ）

**#7（Charts依存の解消）が終わると2つ減る。** `saikyo_mens.html`の移行(2026-09-11)で
`abs.twimg.com`（Xアカウントなし選手の既定アイコン）への**フォールバックの**
依存は解消済み（フォールバックを`img/avatar.svg`に差し替えた）。**ただし
データ側には残っている。** スプレッドシートの画像URLとして
`abs.twimg.com/sticky/default_profile_images/...`が`jpml_pros.html`に11件、
`saikyo_results.html`に2件、計13件焼き込み済み（#135）。`resource_efficiency.html`
の静的SVG化(2026-09-11)では、外部JS(`gstatic.com`)自体が丸ごと不要になった
（グラフ系6ページで唯一、外部JSを一切読まないページになった）。
`houou_leagues.html` / `ouka_leagues.html`の型C静的化(2026-09-11)で
`gstatic.com` / `docs.google.com` への依存はさらに2ページ分解消した。

**`gstatic.com` を消すには、#111（型B 3ページ）と#141（ランキング3ページ）
の両方で Google Charts を使わない判断が要る。** ランキング3ページ
（houou_ranking / ouka_ranking / wrc_ranking）も `gstatic.com/charts/loader.js`
を読んでいるため、型Bだけ静的化してもランキング3ページで Charts を
使い続ける限り `script-src` から `gstatic.com` は外せない。

- どちらか一方でも据え置けば `gstatic.com` は残り、#9 のCSPは
  `script-src` に `gstatic.com` を許可したまま確定することになる
- 逆に言えば、**片方だけ移行しても CSP は1行も変わらない。** #111 と
  #141 は個別に判断できるが、CSPへの効果は両方そろって初めて出る
- したがって、どちらかを据え置くと決めた時点で、もう片方を#7の
  対象から外す判断も成り立つ（ただしランキング3ページのインライン
  イベントハンドラだけは別途消す必要がある。#9に記録済み）

方針は#111・#141で判断する。

**生成済みページの `<img src>` に含まれる外部ドメインは、選手のプロフィール
画像7ドメインだけではない。** `jpml_test.html` は `img.youtube.com`（12件）と
`ron2.jp`（22件）、`resource_logs.html` は `pbs.twimg.com`（全2,630件。
Xの画像への直リンクで、再配信ではない）、`video_live.html` は
`img.youtube.com`（2,332件）に加えて `hayabusa.io`（1件。OpenREC配信回の
サムネイルCDN）を使っている。#9でCSPの`img-src`を書くときは、生成済み
HTMLから実際に使われているドメインを機械的に洗い出すこと（すでに
`img.youtube.com` / `ron2.jp` / `pbs.twimg.com` / `hayabusa.io` が
判明している）。

### タスク管理: 着手中コメントのルールの説明

（移動元: handover.md「3. 作業の進め方」→「タスク管理」の箇条。ルールの本文は CLAUDE.md「issueの着手ルール」節）

- **issueに着手したら、コードを触る前に当該issueへ「着手中」のコメントを
  残す（#157）。** セッションを複数並行させると同じissueの二重着手が起きる。
  他セッションの状況を知る手段はissue上のコメントしかないため、これを
  着手宣言として使う。コメントにはセッションのURLを含める（誰の宣言か
  分からないと引き継ぎも取り下げも判断できない）。中断・放棄したときも
  その旨を残す。着手前には他セッションの宣言が無いか確認する。
  ルールの本文は `CLAUDE.md` の「issueの着手ルール」節にある。
  実例として#127（型C）が2セッションで二重着手された（片方が
  ネットワーク制約で停止していたため衝突は免れたが、偶然だった）

### 「最終更新」から外した行（2026-09-26）

（移動元: handover.md 冒頭の「最終更新」。3行以内の規則で、#440 の行を足すときに最も古い行を移した。最新の状況は handover.md 5章の #222 の行）

- タイトル戦（`title/`）は本番に入った（未公開・noindex）。次は #356 → 公開の判断（5章の #222）。後で進めるもの: #408・#409・#232・#397・#374

---

## CLAUDE.md から（規則は残し、括弧内の経緯だけを移した）

### 「構成」節: GitHub Actions にデプロイを行うジョブが無い理由（#169）

（一度 #169 で `.github/workflows/deploy.yml` として追加したが、Workers
Builds が既に稼働しており不要かつ二重デプロイになるものだったため削除した。
詳細はdocs/handover.md「4-x」と#169のクローズコメント）

### 「データの流れ」節: `content_before` スロットを使うページが無い理由（#102）

（`video_wayhome.html`は#102第2段で`TableConfig`/`render()`自体から離脱したため対象外になった）

### 「データの流れ」節: 型Cの選手選択リストに退会者が出ない判断（#168）

#168で退会者689名・うち#127以前は選べた78名を洗い出し、全員退会済みと確認して対応不要と判断した

### 「データの流れ」節: regenerate-page.yml の差分検知範囲を直す前の挙動（#167）

以前は最終コミット1つ分しか見ておらず、複数コミットをまとめてpushすると途中のコミットの変更を取りこぼした状態でsuccessになっていた

### 各節: 事故・実例の叙述（2026-09-14 に移動、Chat-Ref: CHAT-0914-DR-03、#336）

規則の本文と issue 番号は CLAUDE.md に残し、括弧内の事故の叙述だけを移した。

「ブランチ運用」節:

- 独立した変更を同じブランチに載せた例（YT-03）: YT-03のルール追記がYouTube対応のブランチに載り、単独で先にマージできなくなった（2026-09-14）
- 分岐元の誤り: work/0913-hvが誤って`work/0913-wh2`から分岐した
- 作業ディレクトリの共有（WH-22）: work/0913-whのマージ作業中に、`/workspaces/mj`のチェックアウト先が別セッションのwork/0913-arへ無断で切り替わっていた
- 遅れた土台: `/workspaces/mj`は遅れたまま放置されることがあり、そのHEADから切ると古い土台で作業してしまう（2026-09-14）
- #206: 指示文作成時点では`work/0913-wh2`の先頭は`953c19e`＝マージ済みだったが、実行時には`c54277c`＝未マージまで進んでいた
- #313の後始末: 遅れた`/workspaces/mj`のHEADに対し、警告（"not yet merged to HEAD"）付きで`git branch -d`が通った
- #207: `claude/*`3本をSHAを記録せず削除したため、判定の正しさを後から検証できない状態だった。削除するとリモートから参照が消え、誤って未マージのブランチを消していた場合も復旧できなくなる
- #209の後始末: `work/0913-hv`の元`feae0e3`が、積み直し後`2deb7d4`として`cloudflare`に入っていた
- CHAT-0914-WH-05 のマージ: 取り込みの直前に `cloudflare` の先頭が 61ef850 から fe99a60 へ進んでいた。push の直前に再 fetch し、`cloudflare` が HEAD の祖先であることを確認してから push した（handover.md「複数セッションの並行作業」から移動）
- #313 の作業中: 他セッションのマージ（#312）と handover.md の追記（a1fbffe）が入っていた（handover.md 3章「チャット側から渡された指示と Chat-Ref」から移動）

「Chat-Ref」節:

- CHAT-0914-W2-01: 指示文がissueの決定より狭い範囲のまま実装されるのを、着手前に止めた
- 2026-09-14: work/0914-ytのhandover.md・CLAUDE.md追記や、別セッションによる#265の実装・マージに、作業の途中まで気づけなかった
- #313の作業: チャット側がhandover.mdだけを読んで指示文を書き、CHAT-0914-LK-03で`git switch cloudflare`、LK-04・LK-06で`/workspaces/mj`での`git pull`を指示した。また`-D`の扱いが、別セッションが入れた記述と食い違った。土台のSHAを決め打ちした指示文で停止した

「issueの着手ルール」節:

- CHAT-0914-W2-07: 起票しようとした内容が#265で既に対応・マージ済みだったため見送った
- #157: #127（型C）が2セッションで二重に着手された。片方がネットワーク制約で停止していたため実装の衝突は免れたが、偶然だった

「コミットのルール」節:

- #157: 型C（#127）のCSS 43行が無関係な#152（牌効率ページの見出しフォントサイズ）のコミット `4738d8d` に混入し、`git blame` / `git log -- style.css` が#152を指す状態になった。表示は壊れなかったが、履歴が後から読む人を誤誘導する

2026-09-19 に CLAUDE.md から移した分（Chat-Ref: CHAT-0919-LW-03、#297）:

「引き継ぎ」節:

- CHAT-0915-SK-27・NT-01: 起動時に読み込まれた古い版の CLAUDE.md の、削除済みの記述を根拠に誤った報告をした

「判断・作業の原則」節:

- #310 の検証と #314 の初案が、修正前のコードでも通る検証だった（2026-09-14）

「ブランチ運用」節:

- `/workspaces/mj` の `pull --ff-only` を3条件つきで許した理由（CHAT-0915-NT-04）: セッションは`/workspaces/mj`で作業しないため、放っておくとマージのたびに遅れ、起動時に読み込まれる CLAUDE.md も古くなる。
  `--ff-only`はブランチを切り替えず、未コミット変更と衝突するときは git が拒否するため、禁止の元の危険（WH-22 のブランチ切り替え、他セッションの未コミット変更の破壊）は3条件で防げる
- 新規ワークフローの実例: `cleanup-logs.yml` は dry-run 既定・`SCHEDULE_ENABLED: 'false'` でマージし、`cloudflare`での手動 dry-run を確かめてから `'true'` にした（HT-08→HT-09）
- CHAT-0918-HT-06: 新規ワークフローは `gh workflow run --ref <作業ブランチ>` が404になることを知らずに着手し、中断した

「Chat-Ref」節:

- 作業の再開: CHAT-0916-TT-07→TT-08、TT-09→TT-10（2026-09-18）で、コミットのある Chat-Ref を新しい番号で貼り直した
- 識別子の重複: `CHAT-0916-OG` が2つのチャットセッションで重複しかけた（CHAT-0916-SY-02 で確認の規則を追加）
- 0章ゲート: #336 の DR-01 で、work/saikyo-pages が同じ節を書き換え中だったため停止した
- 指示文の事実と実物の食い違いで中断して報告する規則は、SK-14・SK-16・SK-28・NT-01 で機能した

「作業ログ」節:

- 着手時のログの push: 「着手時」を「最初の作業の区切り」と読むと、並行して始めた調査や最初の変更の後ろに push が回る。
  CHAT-0915-SK-26・NT-01 で続けて発生した（ログを作るのと同じ一括で調査も始め、ログのコミットを後の変更とまとめようとした）
- `## 報告`の「ログ」項目（CHAT-0918-TP-21）: ブランチ名は指示ごとに変わり（`work/0918-tp`・`tp11`・`tp18` など）、チャット側がログを探すのに手間取ったため足した
- ターミナルへの最終報告の行数: 2026-09-16 に CHAT-0916-PL-02 で決め、3行は 2026-09-17（CHAT-0917-HT-04）、4行は 2026-09-19（CHAT-0919-BD-02）
- 最終報告の2行目: worktree のパスにしないのは、作業完了後に削除して無効になるため（URL はブランチ削除後もマージ先で有効。2026-09-18、CHAT-0918-HT-05）。
  URL の直後に全角文字（「（ブランチ: …）」等）を続けると、その文字まで URL とみなされ、クリックで404になった（BD-01）

「コミットのルール」節:

- CHAT-0916-TT-02: シェル経由で書いたログに、展開されたバッククォートの実行結果（gh のヘルプ）が入った

「CLAUDE.md / handover.md の更新ルール」節:

- 「作業ログ」節の着手時の push は、手順の形では SK-26・NT-01 で続けて守られず、順序（「〜より先に行う最初の手順」）で書き直した後は SK 系で守られている（例外は OG-02 の1件）

---

## docs/notes/chat-side-operations.md から（2026-09-21 に移動、Chat-Ref: CHAT-0921-SZ-03、#421）

規則は `docs/notes/chat-side-operations.md` が正。ここには、その規則がこうなった事例（どの Chat-Ref で何が起きたか）と、
確かめた日付・実測値を移した。小見出しは移動後の同ファイルの見出しに対応する。

### 冒頭「この文書には容量の上限がある」

- **新しい項目でなく既存項目へ統合する**: MD-08 は警告域まで残り149バイトの状態で、既存項目への統合ではなく新規の箇条書き4件を
  足し、警告域を331バイト超えた。当てにできる統合候補（MD-05 の報告にある3件）は、MD-05 の時点で「意味を変えずに統合できない」
  として見送り済みで、実際には使えなかった（2026-09-22、CHAT-0922-MD の2回目の振り返り）
- **サイズは作業する worktree の上で測る**（規則は CLAUDE.md「CLAUDE.md / handover.md の更新ルール」へ移設）: MD-08 は着手時の
  chat-side-operations.md のサイズを、worktree を切る前の共有ディレクトリ `/workspaces/mj`（`origin/cloudflare` より27コミット
  遅れた HEAD）で測り、25,683 バイトと記録した。実際の worktree の起点での値は 26,475 バイトで、増分の見積もりが
  +1,571 バイト分ずれていた（MD-09 で訂正。2026-09-22、CHAT-0922-MD の2回目の振り返り）

### 「Claude for Chrome の使い方」節

- **ブラウザを使う前にこの文書を読む**（2026-09-18、CHAT-0918-TK-03）: TK では読む前にブラウザを使い、出力のブロック（`?` `&` `=`）に後から当たった
- **Claude プロファイルを使う**（2026-09-19、CHAT-0918-TL）
- サンドボックスからの `git clone` / `fetch` / `api.github.com` の遮断を確かめたのは 2026-09-13（#211）

### 「1つの確認につき、ブラウザの操作は1回まで」

- 一度のタイムアウトで会話中のブラウザ利用をやめない根拠: LW（2026-09-19）では、タイムアウトが4回あった同じ会話の中で、
  その後の別の確認の get_page_text は3回とも成功した
- 応答しなくなる事象の実例: navigate は通るが get_page_text / screenshot / find が4分でタイムアウトする
  （2026-09-14 発生、同日中に復帰。契機は不明。Claude Desktop の再起動では直らなかった）
- 待つより切り替えるほうが速い: PL（2026-09-16）ではタイムアウトのやり直しで往復が増えた
- タブを閉じる操作がタイムアウトしやすいのは CHAT-0918-TL で確認

### 「ログを読めなかったとき」

- 節の由来は 2026-09-17（CHAT-0917-HT-03）。所在の確かめ方は 2026-09-18（CHAT-0918-HT-11）
- 申告をそのまま信じない根拠: CHAT-0918-TL-04 で、完了の連絡に実行の痕跡が無かったことが一度あった（チャット側の申告）
- 最終報告2行目の URL をそのまま使う根拠: CHAT-0918-HT-05。指示文のブランチ名も最終報告か比較 URL から取る（2026-09-19、CHAT-0918-TN-05）
- ブランチ名を推測しない根拠: 2026-09-18 に、指示文が貼られないまま完了と伝えられ、チャット側がブランチ名を推測して所在を探し回った実例が2回ある

### 「GitHub のファイルを読むとき」

- raw 表示: handover.md（約19,000字）を1回の get_page_text で取れた（2026-09-18、CHAT-0918-TK-01）
- blob 表示: CLAUDE.md（43,989バイト）・handover.md・chat-side-operations.md・instruction-template.md と issue #297 の全文を、
  各1回の get_page_text で取れた（2026-09-19、CHAT-0919-LW。このとき raw 表示は試していない）
- `?plain=1` 付きの blob 表示がメタ情報しか返さないのも同じとき。Markdown 以外（`.py` など）は `?plain=1` なしの blob 表示でも
  メタ情報（行数・サイズ）だけだった（2026-09-21、CHAT-0921-RN。raw 表示は試していない）
- JavaScript から raw を fetch できることの確認は 2026-09-18。2026-09-19 の LW では8ファイルのバイト数・行数を1回で集計できた
- javascript_tool の戻り値の切り詰め: 2026-09-19（LW）は約1,000字で `[TRUNCATED]`。それ以前は約1,400字（`?plain=1` の本文の読み出し）・
  1,900字以下（作業ログの切り出し、CHAT-0918-TH）で返せたことがあり、上限は一定でない
- 重い処理の打ち切り: 104本のログを連続で fetch する処理は45秒で打ち切られた。作業ログの「## 報告」節だけを取り出す使い方は、
  試したがタイムアウトし、まだ確かめられていない。TK（2026-09-18）では、複数ファイルを連続で fetch する JavaScript が
  打ち切られた後、次の JavaScript とタブを閉じる操作が4分のタイムアウトになった（計3回）。LW（2026-09-19）でも、
  8ファイルの集計の直後に docs/logs のツリー表示を開く操作が4分でタイムアウトした。いずれも因果は未確認
- Issues の検索結果の一覧を JavaScript で拾う方法は CHAT-0918-TL で確認
- ブランチ名で開いた内容が古いまま返った実例: 2026-09-12 に、push 後も古い内容が返り、**push 済みの変更を「未反映」と2回誤報した**
  （#76のクローズ、handoverの最終更新行。当時は raw URL）
- 作業ログの読み方の使い分け: 2026-09-21（CHAT-0921-DJ の振り返り）は、`?plain=1` の画面で javascript_tool を使う読み方が
  4回続けてタイムアウトし、`?plain=1` を付けない表示 + get_page_text では2回とも読めた。2026-09-18（CHAT-0918-TH）には
  get_page_text がタイムアウトし、javascript_tool で「## 報告」以降を切り出して読めた例もある
- タブのグループが会話のターンをまたぐと無効になること、JavaScript から work/ ブランチのログも読めることは 2026-09-18（CHAT-0918-TH）で確認

### 「確認用 URL」

- 節の由来は 2026-09-18（CHAT-0918-TH-08）

### 「セッション識別子の確認」

- タイムアウト時に受け手側の確認へ委ねてよいと決めたのは 2026-09-17（CHAT-0917-HT-04）

### 「Claude Code とのやり取り」

- 「ログは `## 報告` だけで足りる」は 2026-09-19（CHAT-0918-TP-21）
- **申送りでは追記先の残り容量を測らせる**: `docs/notes/chat-side-operations.md` は振り返り→申送りのたびに項目が足され、
  2026-09-19 の 25,520 から 2026-09-21 の 34,557 へ2日で膨らんだ。#421 の整理（CHAT-0921-SZ-03・SZ-04）で 24,805 へ戻し、
  上限（警告 26,624 / 失敗 28,672）を入れた（CHAT-0921-SZ-06）。SZ-09 の時点で警告域まで 1,391 バイト
  （2026-09-21、CHAT-0921-SZ の振り返り）

### 「書く前に実物で確かめる」

- **最初の指示を実物で確かめる**（CHAT-0916-SY-01〜05）: 「帰り道も build_ogp_image.py を使う」「識別子 OG は空いている」
  「SK-39 はマージ済み」を確かめずに書き、いずれも誤っていた。既存の仕組みの存在も同じで、TL-07 は
  「既存のテストの置き場所に合わせる」と書いたが、リポジトリにテストは無かった（受け手側が止めて報告した）
- **確かめられないまま指示を書かない**: MT-01（2026-09-21）は、#384 の本文・コメントをチャット側で読めないまま
  「#384 に着手する前の調査」の指示を書いたが、**実物の #384 は既に完了・クローズ済み**で、
  受け手は「調査する対象が無い」として手順の途中で中断した。続く MT-02・MT-03 は撤回されて欠番になり
  （チャット側の申告。コミットもログも残っていないことは確認した）、指示が組み直されるまで3本が無駄になった。
  ブラウザが使えないときは、書く前に平野さんに issue ページの PDF などを頼む（2026-09-21、CHAT-0921-MT の振り返り）
- **マージ・クローズの指示の前の確認**: TN-03 は、TP-07 がマージ・クローズ・handover の更新を済ませた後に出され、中断した
  （2026-09-19、CHAT-0918-TN-05）
- **関連 issue の洗い出しと検索語**: LW は「CLAUDE.md handover」だけで探して、上限の見直しの #200 を見落とし、
  CHAT-0919-LW-08 が着手時に止まった
- **運用ルールの open issue 検索**: #328、CHAT-0914-WF-04
- **`docs/notes/` の最近の変更を読む**: SY-06 は、直前に `docs/notes/ogp.md` へ入った「中身を差し替える画像はファイル名を変える」と
  食い違う指示（ファイル名は変えない）を出して止まった
- **同じ日に他セッションが変えている領域**: LP-08 の確認ポイント7（title/ の前後の期の送りにそろえる）は、書いた後に
  title/ 側が TP-15 で送りを外したため、LP-10 が前提の食い違いで止まった（CHAT-0919-LP-18）
- **インフラやデプロイの提案**: TH-07（2026-09-18）は Cloudflare Pages を前提に「プレビューが作られるか調べる」issue を書かせたが、
  実物は Workers Builds で、プレビューは #38・#114 で対応済みだった（受け手側が止めて報告した）
- **会話の記憶から断定しない**: #319 の作業（2026-09-14〜17、指示45本）では、チャット側が記憶から断定して誤った例が5回あった。
  issue の中身、ログの状態、通知の到達、他セッションの意図、報告の数値の読み違いで、いずれも実物を見れば分かることだった。
  例: SK-42 の「SK-41 のログは空」（続きは未マージの `work/saikyo-pages` にあった）、SK-44 の「エラーメールを送る仕組みがあるはず」（実際は issue の通知）
- **確かめた場所を正しく書く**: GC-24 は、平野さんのスクショでチャット側が見た画面の場所を「#130 に記録のあるもの」として書き、
  受け手が「#130 に記録が無い」と報告した。GC-17 は、選択肢の意味を確かめていない段階の解釈（「9/13 の設定に戻した」）を
  #130 と handover に書かせ、GC-21 で訂正した（2026-09-21、CHAT-0921-GC の振り返り）
- **クローズ済み issue の決定を見落とす**: MD-02 は、chat-side-operations.md を「ブラウザを使う前」「指示文を書く前」で
  2ファイルに分ける前提の指示を書いたが、この案は #421 のやること3で「行わない」と既に決着していた。
  クローズ済み issue の検索で見つけて止まったため実害は無かったが、指示文の作成自体が無駄になった。
  MD-04 も同様に、「書く前に実物で確かめる」節の事例をarchiveへ移す指示を書いたが、
  その移動は #421（CHAT-0921-SZ-03）で既に完了済みだった（2026-09-22、CHAT-0922-MD の振り返り）
- **前の指示の前提を写すときは変更範囲を確かめ直す**: MD-10 は「マージで動く自動処理: docs/ と CLAUDE.md のみの変更なので、
  Workers Builds と自動コミットは動かない見込み」という前提を MD-06 の同種の記述から写して書いたが、MD-06 の変更対象は
  docs/ 配下のみだったのに対し MD-10 は CLAUDE.md を含んでいた。CLAUDE.md は `docs/` 配下ではないため、
  実際には Workers Builds の check-run が1回発生した（表示に影響する変更は無く実害なし。2026-09-22、CHAT-0922-MD の2回目の振り返り）

### 「平野さんの判断とマージの許可」

- **読まれない欄は効果が無い**: MD-01 は「推奨モデル」欄を指示文の雛形に追加したが、平野さんが指示文を
  読まずに貼るという前提と噛み合わず、MD-07 で同じ日に外し、チャット本文で直接伝える運用に変えた
  （2026-09-22、CHAT-0922-MD の振り返り）
- **プレビューの確認・決定をチャット本文にも書く**: 2026-09-21、CHAT-0921-SG-11 の振り返り
- **チャット側の提案を「平野さんの判断」として書かない**: GC-08（#11 の維持）・GC-09（通知先を #304 に）・GC-12（マージしてよい）・
  GC-17（再確認の日付）がこの形になった（結果としては食い違わなかった。2026-09-21、CHAT-0921-GC の振り返り）。
  SZ でも3回あった（SZ-03「S7〈分割〉は行わない」、SZ-06・SZ-07「CLAUDE.md の上限は変えない」）。
  後の2回はチャット本文で断ったが、平野さんは指示文を読まずに貼るため防げない。**現状維持のように当然に見えることも同じ**で、
  これを機に指示文の欄を「決定」と「前提」に分けた（2026-09-21、CHAT-0921-SZ の振り返り。`docs/instruction-template.md`）
- **判断の日付**: 2026-09-19、CHAT-0918-TN-05
- **「添付した」と書く前に確かめる**: GC は「添付は GC-05 です」と2回書いて添付しておらず、平野さんに求められるまで気づかなかった
  （2026-09-21、CHAT-0921-GC の振り返り）
- **公開の時期を確かめる**: LP-14（/live の正式公開）は作った後に取り下げになり、代わりに LP-15 で公開の条件を記録した
  （2026-09-19、CHAT-0919-LP-18）
- **報告の「〜は不明」を handover と突き合わせる**: GC-01 の報告の「#130 は何を待っているか読み取れない」をそのまま伝えたが、
  handover 5章に「9/15以降に旧トグル廃止後の維持を確認してクローズ」とあった。平野さんの指摘で気づき、
  この確認で Training の設定が変わっていたことが見つかった（2026-09-21、CHAT-0921-GC の振り返り）
- **事前のマージ許可**: マージだけの指示を別に出すと1往復増える（CHAT-0916-PL-04→05）
- **マージ用の指示文を添える**: 例は CHAT-0919-LW-07・LW-10
- **片付けを関係のない検証に条件づけない**: CHAT-0921-RN-03 は作業ブランチの削除を `check-ron2-images.yml` の手動実行の成功に
  条件づけたため、ワークフローが落ちたときにブランチ自体に問題が無いのに片付けまで止まり、指示が1本増えた

### 「指示文の書き方・渡し方」

- **「ブランチ運用」節を読む**: #313、CHAT-0914-LK-03/04/06。作業ブランチ名を具体的に書いて規則と違う名前を指定した例は CHAT-0916-PL-01
- **ブランチ名を名指ししない**: CHAT-0917-RK-02 で起きかけた
- **途中切れ**: CHAT-0916-XC-04 は途中で切れていた。確認文は CHAT-0916-XC-07 の「この指示文はここで終わり。」から改めた
- **3項目程度まで**: SK-28 は手順3で止まり、4〜8を SK-29 が引き継いだ
- **配った後の指示を直すとき**: CHAT-0919-LP-05
- **ほかの実行中の指示に触れない**: LP-02 は「5名以上の行の有無は CHAT-0919-LP-01 で調査中」と書いたが、LP-01 は完了・マージ済みで、
  受け手側が食い違いとして止めた（2026-09-19、CHAT-0919-LP-18）
- **既存ブランチを続ける指示に取り込みの指定が無い**: MD-03 は「作業ブランチ: 既存の work/0922-md を続けて使う。
  origin/cloudflare が祖先であることを確かめ、そうでなければ止まる」とだけ書き、merge で取り込んでよいことを書かなかった。
  他セッションのコミットで origin/cloudflare が進んでいたため祖先確認が exit 1 になり、そこで中断した
  （2026-09-22、CHAT-0922-MD の振り返り）

### 「止まる条件と検証の指定」

- **差分 N 行の条件**: #339 の CHAT-0916-XC-02 では、共有ボタンもタイトルを使っていたため差分が1行多く、止まった
- **生成物のマージの基準**: 例（SY-08）は ① 目的の行（og:image）、② 選手写真の srcset の有無、③ 選手写真の画像 URL を許し、
  ②③で変わった写真の URL（_200x200 と _400x400）は HTTP で取得して 200 を確かめ、それ以外が出たら止める、という形
- **スプレッドシートの行数**: CHAT-0916-LV-17・LV-23
- **画像から値を読み取る指示**: #390 の CHAT-0921-DJ-02 は、告知画像に西暦が書かれていないのに読み取り結果の年を突き合わせる作りになり、
  初回の実行が「読み取った年月 2021年9月 が対象の 2026年9月 と違います」で落ちた（2026-09-21、CHAT-0921-DJ の振り返り）
- **通知が出る条件**: CHAT-0921-DJ-03 は「#426 の通知が期待どおりの形であることも確かめる」と書いたが、DJ-02 の報告のとおり
  「追加も照合の失敗も無いときは通知しない」設計だったため、確かめようがなかった（条件は DJ-04 で変えた）
- **ログの事実を issue に書かせるとき**: HG-03（2026-09-19）では、HG-01 のログをチャット側が要約して渡した事実のうち3件
  （パンくずのあるページ、ナビの aria-current、3系統の検索の正規化）が誤っており、受け手側が止めた
- **容量の見込みが外れる**: MD-03 は「事例を移すと容量に余裕ができる」という前提で指示を書いたが、
  MD-04 が確かめると事例は #421 で既に移動済みで、余裕は生まれなかった。MD-07 は「今回は減る方向」と見込んだ修正が
  実際には84バイト増えた（2026-09-22、CHAT-0922-MD の振り返り）

### 「見た目の決め方」

- **実物で見比べる**: #319 では順位ラベル・フォント・対局の切れ目のいずれも、比較画像や実装を見るまで決まらず、決まった後も実物で変わった
  （順位ラベルの三角形はカード幅の30%と40%を切り替えて比べ40%に、対局の切れ目は案3を既定で実装した後に案4へ）。
  使わない案のコードを消すことは SK-31。比較ページのラジオボタンは平野さんの指定で、#364 の帯の4案・回戦ラベルで確認しやすかった
  （2026-09-19、CHAT-0918-TN-05）。title/ の `_compare.html` は帯と回戦ラベルの配色を5案で見比べ、TP-16 で消した
  （2026-09-19、CHAT-0918-TP-21）
- **試作 → 画面イメージ → 判断 → 本実装 → マージ**: #355 の RK-01〜13
- **規則から決めた値も見本を見せる**: PL-04（#344）で AAA の境界値 `#e4e4e4` を見本なしに実装し、本番で重く見えたため
  PL-06（#345）で `#f2f2f2` にやり直した
- **見た目から断定しない**: #339 では、サムネイルに焼き込まれた縦書きの文字を、ページのヒーローの意匠と取り違えた

### 「外部サービスの設定」

- **権限・共有・組織ポリシーの洗い出し**: #379（誕生日カレンダー）では、サービスアカウントが組織外の扱いになり、
  Google Workspace の管理コンソールで予備カレンダーの外部共有を緩める必要があったが、BD-01 の調査にも手順書にも入っておらず、
  設定の途中で分かった（2026-09-20、CHAT-0919-BD の振り返り）
- **要る Secret・変数・共有をすべて列挙する**: #390 の CHAT-0921-DJ-02 は #379 にならった実装だったが、
  カレンダーIDの変数（`DOJO_CALENDAR_ID`）が指示文から抜け、受け手が補って手順書に足した（2026-09-21、CHAT-0921-DJ の振り返り）
- **選択肢の意味を公式で確かめる**: GC は Cloudflare の Configure AI bot policies の Training の「Disallow」と「Block」の違いを
  名前から推測して Block に戻すよう頼んだ。後から公式で調べると、2026-09-15 以降の `Block` は
  「All crawlers, including mixed-use crawlers, are blocked」で Googlebot / Bingbot / Applebot も遮断する設定で、
  方針（検索は許可・学習は拒否）に当たるのは `Disallow AI Training` のほうだった。数時間その状態にしたのち、同じ日に Disallow へ戻した
  （GC-17 → GC-20 → GC-21。2026-09-21、CHAT-0921-GC の振り返り）
- **疑いが出た時点で確かめる**: GC は Block にした直後に「混在クローラーが巻き込まれうる」と気づきながら確認を3日後（9/24）に回し、
  公式で確かめたのは同じ日の後の指示（GC-20）だった。気づいてから確かめるまで、設定はそのままだった

### 「ほかのセッションへの共有」

- 宛先を相手のチャットにする根拠: SY から SK の Claude Code に直接渡る文面にしたため、作業ログが残らなかった
- 解釈を事実として伝えない根拠: SY は srcset の差分を「実行ごとに変わる」と確かめずに伝え、SK セッションから訂正された

## `/titles` 統合の検討と中止（#435、2026-09-22）

「タイトル戦」（`title/`）と「放送対局」（`live/`）を1つのページ群（`/titles`）に統合する計画（#435）は、
設計を確定し（CHAT-0922-UT-01・UT-02）、連盟ch→放送対局タブの転記スクリプトを実施した（CHAT-0922-UT-04）段階で
中止した（CHAT-0922-UT-05、平野さんの判断）。

**中止の理由**: 統合後の個別ページに予選から決勝までの動画を詰め込むと情報量が多すぎ、利用者が探しているものを
見つけにくくなるため。`/title` と `/live` は元どおり別のページとして開発を続ける。

**引き継いだもの**: 転記スクリプトの成果（連盟chタブから放送対局タブへの一括転記、695行・印13件、
`scripts/live_seed/ut04/`）と掲載範囲の拡大（鳳凰戦・女流桜花の決定戦、麻雀日本シリーズ・女流プロ麻雀日本シリーズ、
鳳凰位決定戦）は、`/live` の掲載範囲拡大の作業として issue #437（親 #346）に引き継いだ。設計は
`docs/notes/live-page-design.md`「2-1」「3-5」参照。

**効力を失った決定**: UT-01〜UT-04 で決めた `/titles` 向けの決定（大会の集合・期ページの中身・検索の統合・
`/title`・`/live` の廃止とリダイレクト・#356 の分類・#412 の扱い・進行中作業の停止）はすべて効力を失った。
検討時のみ存在した `docs/notes/titles-page-design.md` は削除した。

経過の詳細: `docs/logs/CHAT-0922-UT-01.md`〜`CHAT-0922-UT-05.md`
