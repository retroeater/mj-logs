# CHAT-0922-MD-19

- 着手日時: 2026-09-25
- 対象issue: #440
- ブランチ: work/0922-md
- 着手時HEAD: 67050e91213e0e1cd4668ddd9ff0f3f324c954fe
- 状態: 判断待ち

## 指示

```
<!-- Claude（チャット側）作成の指示文 -->
Chat-Ref: CHAT-0922-MD-19
共通手順: CLAUDE.md「Chat-Ref」「作業ログ」節のとおり（識別子確認 → ログ先行push → 最終報告の最後の行に Chat-Ref）。「ブランチ運用」節は、下の「作業ブランチ」の読み替えのうえで従う。issue とワークフローの操作は gh の代わりに GitHub MCP で行う
作業ブランチ: この指示はクラウドセッション（Claude Code on the web）で実行する。`/workspaces/mj` と worktree は無い。work/0922-md を使う。リモートに無ければ origin/cloudflare から作る。リモートにあってマージ済み（work/0922-md が origin/cloudflare の祖先）なら、origin/cloudflare から作り直す（`git checkout -B work/0922-md origin/cloudflare`）。マージ済みでなければ止まる。作業中に origin/cloudflare が進んだら merge で取り込んでよい（rebase しない）

## 目的
スマホだけで開発を回すために、次の2つを行う。
1. クラウドセッションでの運用を文書にする（今日の #440 の確認で分かったことと、決まった規則）
2. チャット側が private の mj を読めない問題への対処として、ガイド文書を public の mj-logs に写す前に、公開してよいかを点検する
結果は #440 に記録する。

### 決定（2026-09-25、平野さん）
- ガイド文書（CLAUDE.md・docs/handover.md・docs/notes の文書など）も mj-logs に写す。写す前に、公開してよいかを点検する。issue は private のまま
- プレビューの URL は非公開の URL として扱い、ログには書かない。セッションの最終報告（画面に出る返答）にだけ書く
- 最終報告の Chat-Ref の行の直前に、公開ログの URL の行（`ログ（公開）: https://github.com/retroeater/mj-logs/blob/main/logs/<Chat-Ref>.md`）を出す

### 前提（チャット側。平野さんの決定ではない）
- 文書化の案（文面・置き方は実物に合わせてよい）:
  - 新しいノート `docs/notes/cloud-sessions.md` に、クラウドセッションでの読み替えをまとめる。入れる内容: 始め方（リポジトリ・ブランチ・環境 Claude-iPhone）、worktree を使わないこと、作業ブランチの用意（新しく作る／マージ済みなら作り直す／未マージの作業を続けるときは origin/cloudflare が作業ブランチの祖先であることを確かめる）、判定の前の `git fetch --unshallow`、gh の代わりの GitHub MCP（issue の操作、`actions_run_trigger` の入力は文字列、ジョブのログは完了後に読む）、ブランチは削除できず自動削除（マージ済みで24時間後）に任せること、Network access の許可ドメインの一覧、鍵は Actions にだけ置きワークフローを起動すること、`wrangler` の代わりに Workers Builds のプレビューを使うこと、ログの「## 報告」は最後の一致で書き換えること
  - CLAUDE.md: 「ブランチ運用」からクラウドのノートへの参照を1行。上の決定2つ（プレビューの URL・公開ログの URL の行）を、既にある公開の規則の一句と「最終報告の最後の行に Chat-Ref」の記述に合わせて書く。どちらの環境でも同じ規則にする。警告域（30,720）までの残りを測ってから書く
  - `docs/logs/_template.md` の「確認用URL」の書き方を、上の決定に合わせる
  - `docs/instruction-template.md` の「作業ブランチ」の行に、クラウドで実行するときの書き方の例を足す（新しく作る場合と、未マージの作業を続ける場合。向きを取り違えない書き方にする）
  - docs/handover.md の役割分担に、Claude Code がクラウドセッションでも動くことを一句足す。警告域（26,624）までの残りを測ってから書く
  - chat-side-operations.md は残りが少ないので、この指示では変えない
- 点検の案:
  - 対象は CLAUDE.md・docs/handover.md・docs/instruction-template.md・docs/notes の下の全文書
  - 見るもの: 人の個人情報（氏名と結びついた属性、連絡先など）、鍵・トークンの値、非公開の URL（プレビューの URL、Cloudflare のアカウントのサブドメイン、private なスプレッドシートやドライブの URL など）、アカウントの識別子（メールアドレス、Google Cloud のプロジェクト名・サービスアカウント名など）、そのほか公開すると困りそうなもの
  - **このログは public の mj-logs に写るので、見つけた値そのものはログに書かない。** ファイル名・行番号・種類だけを書き、扱いの案（そのまま公開してよい／書き換える／そのファイルは写さない）を添える
  - 点検だけを行い、書き換えや写す仕組みの追加は、この指示では行わない

## 手順
1. 作業ブランチを準備してログを push する。CLAUDE.md・handover.md のサイズを測る
2. 文書化の案に沿って書き、コミットして push する
3. 点検を行い、結果をログに書く。結果を #440 にコメントする（ここにも値は書かない）

## 止まる条件
- リモートの work/0922-md がマージ済みでない
- 書き込むと CLAUDE.md・handover.md が警告域以上になる（書かずに、縮める案を報告する）

## 完了条件
- ログの「## 報告」を CLAUDE.md「作業ログ」節のとおりに書いて push する。「## 報告」を書き換えるときは、ファイルの中で最後に出てくる「## 報告」を使う
- 報告に、`cloud-sessions.md` の全文、CLAUDE.md・handover.md・`_template.md`・`instruction-template.md` の変更の前後、各文書のサイズと警告域までの残り、点検の結果の一覧（値は書かない）を載せる
- この指示では cloudflare へマージしない。状態は「判断待ち」で止める
- 最終報告の Chat-Ref の行の直前に、`ログ（公開）: https://github.com/retroeater/mj-logs/blob/main/logs/CHAT-0922-MD-19.md` の行を書く
- 最終報告の最後の行に Chat-Ref: CHAT-0922-MD-19 を書く

この指示文は「Chat-Ref: CHAT-0922-MD-19 を書く」の行で終わる。途中で切れていたら止まって報告すること。
```

## 経過

- Chat-Ref の重複: `git log --all --grep` で0件。`MD` は MD-19 のため最初の指示ではなく、識別子の確認は対象外
- リモートの work/0922-md は origin/cloudflare と同じ SHA（祖先＝マージ済み）。`git checkout -B work/0922-md origin/cloudflare` で作り直した
- `git fetch --unshallow origin` 済みの完全履歴で判定した
- 0章ゲート: #440 の本文とコメント（MD-13〜18）を読み、指示の範囲と食い違いは無い。未マージの work/ ブランチは無い（`git branch -r --no-merged origin/cloudflare` が空）。origin/cloudflare の直近は MD-18 のコミットで、重なりは無い
- #440 に着手中のコメント: https://github.com/retroeater/mj/issues/440#issuecomment-5833035641

### 手順1: サイズ（着手時、origin/cloudflare 67050e91）

| 文書 | サイズ | 警告域 | 残り |
|---|---|---|---|
| CLAUDE.md | 28,882 | 30,720 | 1,838 |
| docs/handover.md | 22,544 | 26,624 | 4,080 |
| docs/notes/chat-side-operations.md | 26,586 | 26,624 | 38 |

### 手順2: 文書化（コミット 34bbbbc7）

- 材料: #440 のコメントと、cloudflare にある MD-13〜18 のログの `## 経過`（環境名・浅いクローン・gh の不在・ブランチ削除の 403・許可ドメイン・`actions_run_trigger` の入力・`get_job_logs` の 404・「## 報告」の最初の一致による破損）
- `docs/notes/cloud-sessions.md` を新設（全文は報告）
- CLAUDE.md: 「ブランチ運用」にノートへの参照1行。「作業ログ」の公開の規則にプレビュー URL の一句。最終報告の行を「4行」から、確認用（ある場合）・公開ログ・最後の行 Chat-Ref に変えた。「Chat-Ref」節の「最後の行に Chat-Ref」に「その直前の行に公開ログの URL」を足した。Codespace・クラウドで分けず同じ規則にした
- `docs/logs/_template.md`: 「確認用URL」を「URL は書かない」に変え、最終報告の例に公開ログの行を足した
- `docs/instruction-template.md`: クラウドで実行するときの「作業ブランチ」の行の例を2つ（新しく作る／未マージを続ける）。判定の式をそのまま書かせる
- docs/handover.md「役割分担」に一句
- **指示に無いが直した**: `docs/notes/cloudflare.md`「work/ ブランチのプレビュー」の「作業ログの確認用URLにはこの別名 URL を書く」を、新しい規則に合わせて「作業ログには書かず最終報告にだけ書く」に置き換えた（残すと規則が2か所で逆になるため）
- **直していない食い違い**: `docs/notes/chat-side-operations.md` の「work/ ブランチの見た目は…」（88行目付近）が、ログの「確認用URL」にプレビュー URL がある前提で書かれている。指示でこのファイルは変えないことになっており、残りも38バイトのため手を入れていない（報告の「判断が必要なこと」）
- handover.md の「最終更新」は、一句の追記のため変えていない
- `python3 -m unittest discover -s scripts/tests`: 219件 OK

### 手順3: 点検

対象: CLAUDE.md・docs/handover.md・docs/instruction-template.md・docs/notes の23文書（この指示で足した cloud-sessions.md を含む。docs/notes にサブディレクトリ・md 以外のファイルは無い）。
方法: メールアドレス・URL のホスト・長い ID（スプレッドシート・鍵の形・32桁の16進・`env_`/`session_` 等）・`workers.dev`・GCP のプロジェクト・人名と属性の語（退会・住所・電話・本名など）を grep し、当たった行の前後を読んだ。
値そのものはここに書かない。サイトの公開ファイル（`*.html`・`*.js`）に同じ値があるかも `git grep` で見た。

| # | ファイル:行 | 種類 | 扱いの案 |
|---|---|---|---|
| 1 | docs/notes/cloudflare.md:564, 565 | プレビューの URL の形（Cloudflare アカウントの workers.dev サブドメイン。オーナーの姓を含む） | 書き換える（`<アカウントのサブドメイン>` 等） |
| 2 | docs/notes/chat-side-operations.md:88 | 同上 | 書き換える（同時に規則の食い違いも直す） |
| 3 | docs/notes/cloudflare.md:506 | オーナーのメールアドレス（Cloudflare の API トークンの紐づけ先として） | 書き換える（「平野さんのアカウント」等） |
| 4 | docs/notes/handover-archive-2026.md:221 | 同上 | 書き換える |
| 5 | docs/notes/cloudflare.md:285 | Google Cloud のプロジェクト ID（組織のドメイン・プロジェクト表示名・API キーの名前も同じ行の前後） | プロジェクト ID は書き換える。組織のドメインは公開のドメインなのでそのままでよい。キーの名前（値ではない）はそのままでよい |
| 6 | docs/notes/live-channel-write.md:8, 26, 34 | 同じプロジェクト ID と、それを含むサービスアカウントのメールアドレス | 書き換える |
| 7 | docs/notes/live-channel-write.md:41、live-page-design.md:26、title-pages.md:50 | /live 用スプレッドシートの ID（41行目は開く URL の形）。サイトの公開ファイルには無い。gviz で認証なしに読めるシートのため、ID が分かれば誰でも読める | 書き換える（「/live 用スプレッドシート」等） |
| 8 | docs/notes/live-page-design.md:24 | 正本スプレッドシートの ID | そのまま公開してよい（公開中の JS 3本に同じ ID がある。#7 の旧方式でブラウザから直接読んでいる） |
| 9 | docs/notes/dojo-guest-calendar.md:42, 81 | Google カレンダーの ID（文書中で「公開カレンダーのID」と明記） | そのまま公開してよい（公開カレンダーであることは平野さんの確認が要る。サイトの公開ファイルには無い） |
| 10 | docs/notes/books-calendar.md:34, 49, 51 | カレンダー ID・サービスアカウントの形（`...` で伏せてある） | そのまま公開してよい |
| 11 | docs/notes/birthday-calendar.md・live-channel-write.md（サービスアカウントの表示名 `birthday-calendar` 等）・Secret/Variable の名前 | 名前だけで値は無い | そのまま公開してよい |
| 12 | 全体（「平野さん」169か所、GitHub のアカウント名 retroeater） | オーナーの姓・GitHub アカウント。mj-logs の持ち主として既に公開されている | そのまま公開してよい（姓の扱いは平野さんの判断） |
| 13 | docs/notes/dojo-guest-calendar.md:156、saikyo-page-design.md:617 | 連盟のプロの氏名（読み違い・改名・シートの重複行の例として） | そのまま公開してよい（公開サイトに載っている選手名と公表済みの改名。属性は私的なものではない） |
| 14 | docs/notes/handover-archive-2026.md:417 ほか | 退会者の人数（氏名は無い） | そのまま公開してよい |
| 15 | 全体の `https://github.com/retroeater/mj/...`（Actions の run・設定画面の URL） | private リポジトリの URL。他人には 404 | そのまま公開してよい |
| 16 | CLAUDE.md:153 ほか | Claude Code のセッション URL の形（`<ID>`） | そのまま公開してよい（CLAUDE.md でも可としている） |

見つからなかったもの: 鍵・トークンの値（`AIza`・`ghp_`・`github_pat_`・`sk-ant-` 等の形は0件）、電話番号・住所、Cloudflare のアカウント ID・ゾーン ID、Codespace の名前（`<codespace…>` の置き換えだけ）、Google ドライブの URL。

「そのファイルは写さない」とした文書は無い。書き換えが要るのは6文書（cloudflare.md・chat-side-operations.md・handover-archive-2026.md・live-channel-write.md・live-page-design.md・title-pages.md）。
CLAUDE.md・docs/handover.md・docs/instruction-template.md には書き換えの要る値は無い。

## 報告

- 状態: 判断待ち
- ブランチ: work/0922-md（リモートの同名ブランチがマージ済みだったため、指示どおり origin/cloudflare から作り直した。セッションに割り当てられた `claude/cloud-session-docs-review-wm4dge` は使っていない）
- ログ: https://github.com/retroeater/mj/blob/work/0922-md/docs/logs/CHAT-0922-MD-19.md
- 比較URL: https://github.com/retroeater/mj/compare/cloudflare...work/0922-md
- 確認用URL: なし（docs のみ）
- マージ: 未（平野さんの判断待ち。指示どおり cloudflare へマージしていない）
- issue: #440（着手コメントと結果のコメント）
- 判断が必要なこと:
  - 文書化の変更（コミット 34bbbbc7）を cloudflare へマージするか。ドキュメントのみ
  - `docs/notes/chat-side-operations.md` の「work/ ブランチの見た目は…」の項が、ログの「確認用URL」にプレビュー URL がある前提のままで、新しい規則と食い違う。プレビューのサブドメインも含む（点検 #2）。このファイルは残り38バイトで、指示で変えないことになっている。直すなら、既存の記述を縮めてから置き換える指示が要る
  - 点検で「書き換える」とした7件（#1〜#7。6文書）を、mj-logs に写す前に書き換えるか。書き換えるなら、`handover-archive-2026.md` の過去の叙述も対象になる
  - 点検 #9 のカレンダーが本当に公開カレンダーか（文書の記述だけで判断した）
  - 既に mj-logs に写った過去のログにプレビューの URL などが含まれていないか（この指示の対象外で見ていない）
  - 報告の書式を変えたため、チャット側の `docs/notes/chat-side-operations.md` にある最終報告・確認用URL の読み方も合わせて直す必要がある（上の項と同じ指示で扱える）
- 未確認の項目:
  - cloud-sessions.md の「既定で通るドメイン」「追加した許可ドメイン」は MD-13〜15 の実測の転記で、この指示では測り直していない
  - `get_job_logs` の 404 がジョブ完了前だけの現象かは MD-18 の1回の観察による
- エラー: なし

### cloud-sessions.md の全文

````markdown
# クラウドセッション（Claude Code on the web）での読み替え

Codespace の代わりにクラウドセッションで作業するときの、CLAUDE.md の規則の読み替え。
実測の経過は #440（2026-09-25）。ここに無い規則は CLAUDE.md のとおり。
環境の設定は変わるため、記述と食い違ったら実測を優先する（CLAUDE.md「判断・作業の原則」）。

## 始め方

- リポジトリは `retroeater/mj`、環境は `Claude-iPhone`（アカウントにある環境はこれ1つ。「Default」ではない）
- クローンは `/home/user/mj`。セッションには `claude/...` の名前のブランチが割り当てられるが、使わず push もしない。
  作業ブランチは指示文の「作業ブランチ」の行に従う（書き方は `docs/instruction-template.md`）
- `/workspaces/mj` と worktree は無い。セッションごとにクローンが別なので、worktree を作らずクローンの中で直接ブランチを切る。
  CLAUDE.md「ブランチ運用」の `/workspaces/mj` の pull・worktree の片付けは当てはまらない

## 作業ブランチの用意

最初に `git fetch origin` と `git fetch --unshallow origin` を行う（下の「浅いクローン」）。

- **リモートに無い**: `git checkout -b work/<識別子> origin/cloudflare`
- **リモートにあってマージ済み**（`git merge-base --is-ancestor origin/work/<識別子> origin/cloudflare` が真）:
  `git checkout -B work/<識別子> origin/cloudflare` で作り直す
- **未マージの作業を続ける**: `git checkout -b work/<識別子> origin/work/<識別子>` のうえ、
  **`git merge-base --is-ancestor origin/cloudflare HEAD`**（cloudflare が作業ブランチの祖先）を確かめる。
  偽なら `git merge origin/cloudflare` で取り込む（push 済みなので rebase しない）。上のマージ済みの判定と向きが逆なので取り違えない
- 指示文がどれにも当てはまらない状態（例: 新しく作るはずが未マージのものがある）なら止まって報告する

## 浅いクローン

クローンは既定で浅い（MD-13 の時点で `origin/cloudflare` の68コミットだけ）。浅いままだと祖先の判定・`git log --all --grep` による
Chat-Ref の確認・`git branch --merged` が誤る。**これらの判定の前に `git fetch --unshallow origin`** を行う（数秒で済む）。

## gh の代わりに GitHub MCP

`gh` は入っていない。issue・Actions の操作はセッションの GitHub MCP ツールで行う。CLAUDE.md の `gh` のコマンドは、同じ操作の MCP ツールに読み替える。

- issue: `issue_read`（本文・コメント）、`search_issues`・`list_issues`（検索。クローズ済みも見る）、`add_issue_comment`、`issue_write`。
  本文は引数の文字列で渡す（`--body-file` の代わり。シェルを通らないので展開の心配は無い）
- check-run・コミット: `get_commit`、`get_check_run`、`actions_list`・`actions_get`
- ワークフローの起動: `actions_run_trigger`（method `run_workflow`、ref は既定ブランチ `cloudflare`）。
  **入力はすべて文字列で渡す**（`{"dry_run": "true"}`）。起動できるのは既定ブランチにあるワークフローだけ（docs/notes/branch-operations.md「ワークフローを変更したとき」）
- ジョブのログ（`get_job_logs`）は**ジョブの完了後に読む**。実行中は HTTP 404 になる
- 触れるリポジトリはセッションの sources（`retroeater/mj`）だけ。mj-logs への書き込みは拒否される（写すのは `sync-logs.yml`）

## ブランチの削除

セッションの git プロキシがブランチの削除を拒否する（`git push origin --delete` が HTTP 403）。削除はしない。
マージ済みの `work/*` は `delete-merged-branches.yml` が毎日、先頭が24時間より前のものを削除する（削除の記録もワークフローの出力に残る）。
CLAUDE.md「ブランチ運用」の「作業ブランチも削除する」は、このワークフローに任せることで満たす。

## ネットワーク

外への接続はセッションのプロキシを通り、環境の Network access の許可ドメインで決まる。拒否されると
`CONNECT tunnel failed, response 403`（サーバの 403 ではない）。状態は `curl -sS "$HTTPS_PROXY/__agentproxy/status"` の `recentRelayFailures`。

既定で通るもの（MD-13 で実測）: `sheets.googleapis.com`、`www.googleapis.com`、`oauth2.googleapis.com`、`searchconsole.googleapis.com`、
`api.github.com`、`pypi.org`

環境に追加した許可ドメイン（2026-09-25、MD-14・MD-15 で通ることを確認）:

- `docs.google.com`（スプレッドシートの読み込み。再生成に必須）
- `*.googleusercontent.com`（CSV エクスポートのリダイレクト先。フィルタ検知 #432 に要る）
- `www.ma-jan.or.jp`、`ron2.jp`、`x.com`、`pbs.twimg.com`、`www.youtube.com`、`img.youtube.com`、`ryoei.pro`、`thumbnail.image.rakuten.co.jp`

未追加: `openapi.rakuten.co.jp`（books は凍結中）。再生成は `jpml_titles` で確かめた（MD-15）。鍵の要るスクリプトはクラウドでは動かない（次節）。

## 鍵

鍵（`YOUTUBE_API_KEY`・楽天・`ANTHROPIC_API_KEY`・スプレッドシート書き込みの認証など）は環境の変数に置かない。
**GitHub Actions のシークレットにだけ置き、鍵の要る処理はワークフローを `actions_run_trigger` で起動して行う。**

## ローカル確認の代わりにプレビュー

`wrangler` は入っておらず、`wrangler dev` は使わない。表示の確認は、作業ブランチの push で Workers Builds が作るプレビューで行う
（仕組みは docs/notes/cloudflare.md「work/ ブランチのプレビュー」）。URL は check-run「Workers Builds: mj」の出力にある。

- **プレビューの URL は非公開の URL として扱い、ログ・issue・コミットに書かない。ターミナルの最終報告にだけ書く**（CLAUDE.md「作業ログ」節）
- セッションからはプレビューに接続できないことがある（MD-17 では拒否された）。そのときは表示を確かめていないことを「未確認の項目」に書く

## 作業ログ

- push したログは `sync-logs.yml` が public の mj-logs に写す（`work/**` への push でも写る）。書かない情報は CLAUDE.md「作業ログ」節
- **`## 報告` を書き換えるときは、ファイルの中で最後に出てくる `## 報告` を対象にする。** `## 指示` に貼った指示文の中にも
  `## 報告` が出てくることがあり、最初の一致を使うと指示文の途中から後ろを消す（MD-14 のログで起きた。MD-15 で直した）
````

### 変更の前後（差分。`_template.md` の旧文にあったアカウントのサブドメインは伏せた）

```diff
diff --git a/CLAUDE.md b/CLAUDE.md
index 84bc047b..e2c1d516 100644
--- a/CLAUDE.md
+++ b/CLAUDE.md
@@ -93,6 +93,7 @@ This file provides guidance to Claude Code (claude.ai/code) when working with co
   - コミットを分割するときは`git add -p`、または`git diff`で切り出したハンクを`git apply --cached`で部分ステージする
     （`git stash`は共有パスの他セッションの未コミット編集を無言で消しうる、#198）
   - 作業完了後は`git worktree remove`で片付け、作業ブランチも（下記の手順で）削除する
+- **クラウドセッション（Claude Code on the web）には`/workspaces/mj`・worktree・`gh`が無い。読み替えはdocs/notes/cloud-sessions.md**
 - **`cloudflare`へのマージはセッション自身の判断で行わない。** 作業完了を報告し、平野さんが判断する。基準:
   - ドキュメントのみの変更（CLAUDE.md、docs/配下、README等）は、完了を報告したうえでセッションがマージしてよい
   - サイトの表示・生成物に影響する変更（HTML/CSS/JS、`generate_*.py`、データ取得まわり等）は、平野さんが
@@ -152,7 +153,7 @@ This file provides guidance to Claude Code (claude.ai/code) when working with co
   Claude-Session: https://claude.ai/code/session_<ID>
   ```
 - **コミットを伴わない指示（issueの起票・編集のみ、確認のみ）では、関係するissueのコメント末尾に`Chat-Ref:`行を書くこと。**
-- **Chat-Ref付きの指示への報告は、最後の行に`Chat-Ref: CHAT-MMDD-XX-nn`を書くこと。**
+- **Chat-Ref付きの指示への報告は、最後の行に`Chat-Ref: CHAT-MMDD-XX-nn`を、その直前の行に公開ログの URL（「作業ログ」節）を書くこと。**
   トレーラ・issueコメントとは別に、報告そのものにも付ける（チャット側が報告と指示を対応づけるため）。複数の指示に答える場合は全て書く
 - 到達確認は、Claude Code側は`git log --all --grep="CHAT-MMDD-XX-nn" --oneline`。チャット側はブラウザのIssues検索か、Claude Codeへの依頼
   （独立に検証する場合はdocs/notes/chat-side-operations.md「コミット履歴を独立に検証するとき」の`graph.txt`）
@@ -165,6 +166,7 @@ This file provides guidance to Claude Code (claude.ai/code) when working with co
 指示1件ごとの経過と報告を、リポジトリ内のファイルに残す。issueの有無にかかわらず全セッション共通。
 `docs/logs/`は`.assetsignore`で公開されないが、push したログは public の`retroeater/mj-logs`に写る（`sync-logs.yml`、#440）。
 **ログに人の個人情報（氏名と結びついた属性など）・鍵やトークンの値・非公開の URL（Claude Code のセッション URL は可）を書かない。**
+プレビューの URL（Workers Builds の別名 URL など）も非公開として扱い、ターミナルへの最終報告にだけ書く。
 
 - **1指示につき1ファイル。パスは`docs/logs/<Chat-Ref>.md`**
 - **ログのpushは、調査・計測・実装より先に行う最初の手順とする。** Chat-Refの重複確認の直後に、ヘッダと`## 指示`だけのログを
@@ -183,12 +185,14 @@ This file provides guidance to Claude Code (claude.ai/code) when working with co
     URL の直後には全角文字を続けない（改行か半角空白で区切る）
 - ログは作業ブランチにコミットし、`cloudflare`へのマージ時に成果物と一緒に入れる
 - 後の指示で使うスクリプト・中間データの置き場所は docs/notes/session-network.md「作業ファイルの置き場所」（scratchpad は再起動で消える）
-- **ターミナルへ返す最終報告は次の4行だけにする**（平野さんはチャット側へ「`<Chat-Ref>` 完了」とだけ伝える）:
+- **ターミナルへ返す最終報告は次の行だけにする**（平野さんはチャット側へ「`<Chat-Ref>` 完了」とだけ伝える）:
   - 1行目: 状態（完了 / 判断待ち / 中断）
   - 2行目: ログの GitHub URL だけ（`## 報告`の「ログ」と同じ。形は`docs/logs/_template.md`）。
     **URL の直後に文字を続けない**（続く文字まで URL とみなされ404になる）
   - 3行目: `ブランチ: <ブランチ名>`（「（マージ後に削除済み）」等の補足もこの行に書く）
-  - 4行目: `Chat-Ref: CHAT-MMDD-XX-nn`（複数の指示に答える場合は全て書く。「Chat-Ref」節）
+  - 確認用のプレビューがあるときだけ: `確認用: <URL>`
+  - `ログ（公開）: https://github.com/retroeater/mj-logs/blob/main/logs/<Chat-Ref>.md`
+  - 最後の行: `Chat-Ref: CHAT-MMDD-XX-nn`（複数の指示に答える場合は全て書く。「Chat-Ref」節）
 
   判断が必要なこと・エラーを含め、詳細はログの`## 報告`に書き、ターミナルには出さない
 - **例外として、次の2つはログに届かないためターミナルに内容を書く:**
diff --git a/docs/handover.md b/docs/handover.md
index fef6a5b3..1cef4803 100644
--- a/docs/handover.md
+++ b/docs/handover.md
@@ -113,6 +113,7 @@ Google Charts依存（#7の対象）・静的なページ の4つ。**件数の
 
 Claude Codeは Codespace のターミナルで動いている（`/workspaces/mj` で `claude`）。
 `gh` が認証済みのため、issueの開閉やラベル操作がそのまま通る。
+クラウドセッション（Claude Code on the web）でも動く（`gh` の代わりに GitHub MCP を使う。docs/notes/cloud-sessions.md）。
 
 **Rebuild 後は `post-create.sh` が Claude Code を自動で入れる**（#212）。`.devcontainer/` を変えたときの反映手順と、
 codespace を作り直す前の確認は `docs/notes/session-network.md`「Rebuild と Claude Code」。
diff --git a/docs/instruction-template.md b/docs/instruction-template.md
index b2b33b4f..0cbf85be 100644
--- a/docs/instruction-template.md
+++ b/docs/instruction-template.md
@@ -4,6 +4,9 @@
 
 - 作業ブランチ名は具体名を書かず「『ブランチ運用』の規則どおり」とする
 - 既存の作業ブランチを続けて使う指示は、「Chat-Ref」の次に「作業ブランチ: origin/cloudflare を起点に切った既存の work/<識別子> を続けて使う（〜のため）。着手時と作業中に origin/cloudflare が進んでいたら merge で取り込んでよい（push 済みなので rebase しない）」の1行を入れる。取り込みの可否を書かないと、祖先確認だけで中断する
+- クラウドセッション（Claude Code on the web）で実行する指示は、`/workspaces/mj` と worktree が無いため「作業ブランチ」の行を次のどちらかにする（読み替えは docs/notes/cloud-sessions.md）。判定の向きが2つで逆なので、式をそのまま書く
+  - 新しく作る: 「作業ブランチ: クラウドセッションで実行する。work/<識別子> を使う。リモートに無ければ origin/cloudflare から作る。リモートにあってマージ済み（`git merge-base --is-ancestor origin/work/<識別子> origin/cloudflare` が真）なら `git checkout -B work/<識別子> origin/cloudflare` で作り直す。マージ済みでなければ止まる」
+  - 未マージの作業を続ける: 「作業ブランチ: クラウドセッションで実行する。未マージの work/<識別子> を続けて使う（〜のため）。`git checkout -b work/<識別子> origin/work/<識別子>` のうえ、`git merge-base --is-ancestor origin/cloudflare HEAD` が偽なら merge で取り込んでよい（rebase しない）。リモートに無ければ止まる」
 - SHA を固定して書かない。土台や比較対象は「その時点の `origin/cloudflare`」と書く
 - 定型部分は2行目の「共通手順」1行にまとめ、個々の手順を文章で書き直さない（写し間違いと途中切れを減らすため。2026-09-17）
 - 運用ルールの変更を含む指示は、手順1を「同じ論点の open issue を検索し（クローズ済みも含めて確認）、あれば止まって報告する」に固定する。チャット側はブラウザで検索できないことがあり、受け手側の検索で代替するため（HT-02 で #294 を検出した実例）
diff --git a/docs/logs/_template.md b/docs/logs/_template.md
index 1351f89c..caf44f68 100644
--- a/docs/logs/_template.md
+++ b/docs/logs/_template.md
@@ -14,7 +14,7 @@ CLAUDE.md「作業ログ」節が正。ここには**書く時点で読めば足
 | ブランチ | 実際に使ったブランチ名。指示文の指定と違う場合はその旨と理由 |
 | ログ | `https://github.com/retroeater/mj/blob/<実際に使ったブランチ>/docs/logs/<Chat-Ref>.md`。マージ済みなら `blob/cloudflare/`（CHAT-0918-TP-21） |
 | 比較URL | `https://github.com/retroeater/mj/compare/cloudflare...<ブランチ>` |
-| 確認用URL | コード・生成物を変えた作業では、Workers Builds のプレビューの別名 URL（ブランチ名の `/` を `-` にした `https://work-<識別子>-mj.<アカウントのサブドメイン>.workers.dev/<ページ>`。ログイン不要・noindex）。ビルドが走らなかった・失敗したときは、その旨と Codespace の確認用 URL を書く。docs のみなら「なし」（CHAT-0918-TH-08。詳細は `docs/notes/cloudflare.md`「work/ ブランチのプレビュー」） |
+| 確認用URL | **URL は書かない**（プレビューの URL は非公開として扱い、ターミナルへの最終報告にだけ書く。CLAUDE.md「作業ログ」節）。コード・生成物を変えた作業では「プレビューあり（URL は最終報告）」と、確認したページ。ビルドが走らなかった・失敗したときはその旨（代わりに使った確認用 URL も最終報告にだけ書く）。docs のみなら「なし」（詳細は `docs/notes/cloudflare.md`「work/ ブランチのプレビュー」） |
 | マージ | 済（マージコミットのSHA）/ 未（平野さんの判断待ち） |
 | issue | 番号（起票・クローズしたものを含む）。無ければ「なし」 |
 | 判断が必要なこと | 箇条書き。無ければ「なし」 |
@@ -28,7 +28,7 @@ CLAUDE.md「作業ログ」節が正。ここには**書く時点で読めば足
 **`## 報告` に `Chat-Ref:` の行は置かない。** どの指示の報告かは「ログ:」の URL に含まれる Chat-Ref で見分ける（#419 (b)）。
 `Chat-Ref:` を書くのは、コミットのトレーラ・issue のコメントの末尾・**ターミナルへの最終報告の最後の行**の3か所。
 
-## ターミナルへ返す最終報告（4行）
+## ターミナルへ返す最終報告
 
 2行目の URL は `## 報告` の「ログ」と同じもの。未マージなら
 `https://github.com/retroeater/mj/blob/<実際に使ったブランチ>/docs/logs/<Chat-Ref>.md`、マージ後なら `blob/cloudflare/` 以下
@@ -38,9 +38,12 @@ CLAUDE.md「作業ログ」節が正。ここには**書く時点で読めば足
 完了
 https://github.com/retroeater/mj/blob/cloudflare/docs/logs/CHAT-MMDD-XX-nn.md
 ブランチ: work/MMDD-xx（マージ後に削除済み）
+ログ（公開）: https://github.com/retroeater/mj-logs/blob/main/logs/CHAT-MMDD-XX-nn.md
 Chat-Ref: CHAT-MMDD-XX-nn
 ```
 
+プレビューで確認する作業では、「ブランチ:」の行の次に `確認用: <プレビューの URL>` の行を足す（ログには書かない）。
+
 ## ひな形
 
 ```markdown
diff --git a/docs/notes/cloudflare.md b/docs/notes/cloudflare.md
index c3d3d90b..6759734c 100644
--- a/docs/notes/cloudflare.md
+++ b/docs/notes/cloudflare.md
@@ -564,7 +564,7 @@ Workers & Pages → `mj` → Settings → Builds:
   - `Preview URL: https://<バージョンIDの先頭8桁>-mj.<アカウントのサブドメイン>.workers.dev`（そのバージョン固定）
   - `Preview Alias URL: https://<ブランチ名>-mj.<アカウントのサブドメイン>.workers.dev`（ブランチ名の `/` を `-` にした形。例: `work/0918-th` → `work-0918-th-mj…`）。2026-09-12〜18 の work/ ブランチ24本で同じ形を確認。`.` や大文字を含む名前は未確認
 - ログイン不要で誰でも開ける。`x-robots-tag: noindex` が付く。ブランチを削除した後も開ける
-- 作業ログの「確認用URL」にはこの別名 URL を書く（CLAUDE.md「作業ログ」節、書き方は `docs/logs/_template.md`）
+- この URL は非公開として扱い、作業ログには書かず、ターミナルへの最終報告にだけ書く（CLAUDE.md「作業ログ」節、ログの書き方は `docs/logs/_template.md`）
 - **先頭が docs/ だけのコミットの push ではビルドが走らないことがある**（TH-03 の f7d74e4・TH-04 の 7e601b3 をログのコミットと一緒に push し、check-run が付かなかった）。
   Build watch paths の `docs/**` 除外（#171）が先頭のコミットで判定されている、という推定（未検証、#363）。確実に見たいときは、コードのコミットをログのコミットより先に単独で push する
 
```

### サイズ（コミット 34bbbbc7 の時点）

| 文書 | 変更前 | 変更後 | 警告域 | 残り |
|---|---|---|---|---|
| CLAUDE.md | 28,882 | 29,427 | 30,720 | 1,293 |
| docs/handover.md | 22,544 | 22,693 | 26,624 | 3,931 |
| docs/notes/chat-side-operations.md | 26,586 | 26,586（変更なし） | 26,624 | 38 |
| docs/logs/_template.md | 3,705 | 4,019 | なし | - |
| docs/instruction-template.md | 6,610 | 7,753 | なし | - |
| docs/notes/cloud-sessions.md | - | 6,981 | なし | - |
| docs/notes/cloudflare.md | 44,390 | 44,453 | なし | - |
