# CHAT-0922-MD-17

- 着手日時: 2026-09-25
- 対象issue: #440
- ブランチ: work/0922-md
- 着手時HEAD: 96db73cd
- 状態: 着手

## 指示

<!-- Claude（チャット側）作成の指示文 -->
Chat-Ref: CHAT-0922-MD-17
共通手順: CLAUDE.md「Chat-Ref」「作業ログ」節のとおり（識別子確認 → ログ先行push → 最終報告の最後の行に Chat-Ref）。「ブランチ運用」節は、下の「作業ブランチ」の読み替えのうえで従う。issue の操作は gh の代わりに GitHub MCP で行う
作業ブランチ: この指示はクラウドセッション（Claude Code on the web）で実行する。`/workspaces/mj` と worktree は無い。work/0922-md（マージ済み、クラウドからは削除できない）を origin/cloudflare の先頭に早送りしてから使う（例: `git checkout -B work/0922-md origin/cloudflare`。work/0922-md が origin/cloudflare の祖先でなければ止まる）。作業中に origin/cloudflare が進んだら merge で取り込んでよい（rebase しない）

## 目的
スマホだけで開発を回すための仕組みを2つ、GitHub Actions のワークフローとして作る。
1. ログを public リポジトリ `retroeater/mj-logs` へ写す（チャット側が報告を貼ってもらわずに読めるようにする）
2. マージ済みの `work/*` ブランチを自動で削除する（クラウドのセッションからはブランチを削除できないため）
結果は #440 に記録する。

### 決定（2026-09-25、平野さん）
- ログは mj の Actions から mj-logs へ写す（MD-16 の報告の案 c）。mj 本体は private のまま
- mj-logs にだけ書き込めるトークンを、mj の Actions のシークレット `MJ_LOGS_TOKEN` に登録した
- マージ済みの `work/*` ブランチは、GitHub Actions で自動削除する
- 鍵は Actions のシークレットにだけ置く

### 前提（チャット側。平野さんの決定ではない）
- ログを写すワークフローの案:
  - `docs/logs/` の下が変わる push（cloudflare と `work/**` の両方）で動く。push 時に使われるのは push したコミットにあるワークフローなので、作業ブランチへの push だけで試せる
  - 写すのは、その push で追加・更新されたログだけ（mj-logs の `logs/<ファイル名>`、main へ直接）。**過去のログは一括では写さない**（公開してよいかを点検していないため）
  - cloudflare で削除されたログは、mj-logs にあれば削除する（mj の定期削除に合わせる）。`work/**` の push では削除しない
  - 同時に走ったときにぶつからないよう、concurrency で1本ずつ流す。mj-logs のコミットメッセージには元のコミットの SHA を入れる
  - トークンの値をログや出力に出さない
- ブランチを自動削除するワークフローの案:
  - 毎日1回（schedule）と手動（workflow_dispatch）で動く。schedule と workflow_dispatch は既定ブランチのワークフローで動くため、この指示では動かせない。スクリプトの部分を、削除しない試運転（dry-run）で手元で動かし、対象の一覧を確かめる
  - 対象は、`origin/cloudflare` の祖先になっている（マージ済みの）`work/*` ブランチのうち、先頭のコミットが24時間より前のものだけ。作業中のブランチを消さないための猶予
  - `permissions: contents: write`（Actions の標準トークン）で消す。削除したブランチ名と先頭の SHA を実行ログに出す（CLAUDE.md の「ブランチ削除の記録」に相当）
- 公開の規則の案: CLAUDE.md「作業ログ」節に一句足す。ログは public の mj-logs に写るので、人の個人情報（氏名と結びついた属性など）、鍵・トークンの値、非公開の URL（Claude Code のセッション URL は可）を書かない。CLAUDE.md の警告域（30,720）までの残りを測ってから書く
- ワークフローの置き方・名前は、既存の `.github/workflows/` の書き方に合わせてよい
- `.github/` と CLAUDE.md は `docs/` の外なので、push で Workers Builds の check-run が走る見込み。クラウドから push した作業ブランチにプレビューが作られるかも、あわせて確かめる
- この指示ではマージしない。判断待ちで止め、チャット側が mj-logs に写ったログで内容を確かめてから、マージの指示を出す

## 手順
1. 作業ブランチを準備してログを push する。既存の `.github/workflows/` の書き方、既定ブランチ、CLAUDE.md のサイズを確かめる
2. 2つのワークフローと CLAUDE.md の一句を書いて push する。ログを写すワークフローが作業ブランチへの push で動き、このログが mj-logs の `logs/` に届いたかを確かめる。ブランチ削除のスクリプトは dry-run で動かし、対象の一覧を記録する。push した先頭のコミットの Workers Builds の check-run の結果も記録する
3. 報告を書いて push し、報告まで入った版が mj-logs に届いたかを確かめる。結果を #440 にコメントする

## 止まる条件
- work/0922-md が origin/cloudflare の祖先でない
- 書き込むと CLAUDE.md が警告域以上になる（一句を書かずに報告する）
- ログを写すワークフローが失敗した場合は、止まらずにエラーの全文を記録する

## 完了条件
- ログの「## 報告」を CLAUDE.md「作業ログ」節のとおりに書いて push する。「## 報告」を書き換えるときは、ファイルの中で最後に出てくる「## 報告」を使う
- 報告に、2つのワークフローの全文と CLAUDE.md に足した一句の前後を載せる（チャット側は mj-logs に写ったログで読み比べる）
- 報告に、mj-logs に写った MD-17 のログの URL、Actions の実行結果、dry-run の対象一覧、Workers Builds の check-run の結果を書く
- この指示では cloudflare へマージしない。状態は「判断待ち」で止める
- 最終報告の最後の行に Chat-Ref: CHAT-0922-MD-17 を書く

この指示文は「Chat-Ref: CHAT-0922-MD-17 を書く」の行で終わる。途中で切れていたら止まって報告すること。

## 経過

### 手順1（準備と下調べ）
- `git log --all --grep="CHAT-0922-MD-17"` は0件。work/0922-md（96db73cd）は origin/cloudflare の祖先で、先頭同士が一致していたため `git checkout -B work/0922-md origin/cloudflare` で使用。ログのみのコミット c9549fe5 を push
- 既定ブランチ: `cloudflare`（`git remote show origin` の HEAD branch）
- 既存のワークフロー: `cleanup-logs.yml`（dry-run 入力＋`SCHEDULE_ENABLED` で schedule を dry-run にする型）、`sitemap-lastmod.yml`（concurrency の書き方）に合わせた
- docs/notes/branch-operations.md「ワークフローを変更したとき」「ブランチを削除するとき」を読んだ。新規のワークフローは既定ブランチに無いため `workflow_dispatch` がマージ前に使えない → 削除のワークフローは schedule も dry-run で走る状態（`SCHEDULE_ENABLED: 'false'`）で作った
- CLAUDE.md: 28,603 バイト（警告域 30,720 まで 2,117）。一句を足して 28,882

### 手順2（実装）
- `.github/workflows/sync-logs.yml`: push（cloudflare・`work/**`、paths `docs/logs/**`）で動く。写すのは `docs/logs/CHAT-*.md` のうちその push で追加・更新されたもの。
  `work/**` では `^origin/cloudflare` を付けた `git log` で、cloudflare に入っていないコミットで変わったログだけに絞る（cloudflare を取り込む merge や早送りで他のログを写さないため）。
  cloudflare の push では、cloudflare の履歴で削除され現存しないログを mj-logs から消す（cleanup-logs.yml の削除は GITHUB_TOKEN の push のためこのワークフローを起動しない。次の cloudflare への push で消える）
- `.github/workflows/delete-merged-branches.yml` と `scripts/delete_merged_branches.py`: 毎日 22:53 UTC と手動。マージ済みかつ先頭が24時間より前の `work/*` を `--force-with-lease` 付きで削除（判定後に push されていたら消さない）。`scripts/` と `.github` は `.assetsignore` 済み
- CLAUDE.md「作業ログ」節の一句（前後は報告に載せる）
- コミット: db055cc1（ワークフロー・スクリプト）、f64ec65d（CLAUDE.md）

## 報告

- 状態:
- ブランチ:
- ログ:
- 比較URL:
- 確認用URL:
- マージ:
- issue:
- 判断が必要なこと:
- 未確認の項目:
- エラー:
